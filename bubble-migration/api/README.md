# Bubble API Connector

Documentation of Bubble's API Connector configuration — external APIs
Bubble calls (names, endpoints, auth type). **Never store actual API
keys/secrets here** — document which integration exists and what it's
used for; real credentials belong in each app's environment variables.
