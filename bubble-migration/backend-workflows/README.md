# Bubble Backend Workflows

Screenshots/exports of Bubble's server-side (backend) workflows —
scheduled workflows, API workflows triggered internally, recurring
events. These map to NestJS services, cron jobs, or queue consumers
during migration (see master prompt §15–16).
