# Bubble Plugins

List of installed Bubble plugins and what each one is used for, so the
equivalent library/service can be identified for the rebuilt stack
(e.g. a Bubble email plugin -> a specific email provider integration).
