# Bubble Reusable Elements

Screenshots and notes for Bubble's reusable elements (shared components
used across multiple pages — headers, cards, modals, nav, etc.). One
subfolder per reusable element, mirroring the `pages/` convention.
