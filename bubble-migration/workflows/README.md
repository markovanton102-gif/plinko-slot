# Bubble Workflows (Frontend)

Screenshots and notes for frontend Bubble workflows that aren't scoped to
a single page (e.g. reusable-element workflows, app-level workflows).
Page-specific workflows should instead live under
`bubble-migration/pages/<page>/workflows/`.

Name files after the workflow's trigger/action for traceability, e.g.
`connect-button-click.png`.
