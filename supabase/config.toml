# Minimal local Supabase CLI config, hand-written (Supabase CLI is not installed
# in this environment). Install the Supabase CLI and run `supabase init` in this
# directory to regenerate the full default config; review the diff before keeping it.
project_id = "lawsphere"

[api]
enabled = true
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
major_version = 15

[studio]
enabled = true
port = 54323

[auth]
enabled = true
site_url = "http://localhost:3000"
additional_redirect_urls = ["http://localhost:3000"]
jwt_expiry = 3600
enable_signup = true

[storage]
enabled = true

[realtime]
enabled = true
